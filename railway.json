{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r artist_tour_manager/requirements.txt && cd artist_tour_manager && python manage.py collectstatic --no-input"
  },
  "deploy": {
    "startCommand": "cd artist_tour_manager && python manage.py migrate --no-input && gunicorn artist_tour_manager.wsgi --log-file -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
